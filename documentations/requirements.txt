mkdocs==1.6.1

# Raspberry Pi Desktop / Bullseye i386: MarkupSafe often has no compiled wheels for i686.
# Pin to a release that ships a universal (py3-none-any) wheel to avoid building from source.
MarkupSafe==3.0.0
